dependencies {
    compileOnly project(":server:common")
    compileOnly libs.paper.api
    compileOnly libs.packetevents.spigot

    include libs.afternode.bukkit
}

processResources {
    filesMatching("plugin.yml") {
        expand "version": project.version
    }
}
